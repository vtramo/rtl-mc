current_source_dir = meson.current_source_dir()

abstractions_source_files = [
    current_source_dir / 'PolyhedralAbstraction.h'
]

subdir('omnidirectional')
subdir('general')

project_source_files += abstractions_source_files