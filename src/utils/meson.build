subdir('ppl')
subdir('spot')

current_source_dir = meson.current_source_dir()
utils_source_files = [
    current_source_dir / 'Timer.h',
    current_source_dir / 'utils.h',
]
project_source_files += utils_source_files